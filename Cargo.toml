[package]
name = "vectx"
version = "0.2.8"
edition = "2021"
rust-version = "1.75"
description = "A fast in-memory vector database with full Qdrant API compatibility"
authors = ["antonellof"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/antonellof/vectX"
homepage = "https://github.com/antonellof/vectX"
documentation = "https://docs.rs/vectx"
readme = "README.md"
keywords = ["vector-database", "embeddings", "similarity-search", "hnsw", "qdrant"]
categories = ["database", "data-structures", "algorithms"]
exclude = ["data/", "scripts/", "tests/"]

[workspace]
members = [
    "lib/core",
    "lib/storage",
    "lib/api",
]

[dependencies]
# Core dependencies
tokio = { version = "1.48", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
thiserror = "1.0"
parking_lot = "0.12"
ahash = "0.8"
bytes = "1.0"
uuid = { version = "1.0", features = ["v4"] }

# API dependencies
actix-web = { version = "4.11", features = ["rustls-0_23"] }
actix-cors = "0.7"
tonic = { version = "0.11", features = ["gzip"] }
prost = "0.12"
prost-types = "0.12"

# Storage
bincode = "1.3"
atomicwrites = "0.4"
heed = "0.22"  # LMDB wrapper for fast persistence
nix = { version = "0.29", features = ["process"] }

# Utilities
clap = { version = "4.5", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
log = "0.4"

# Internal crates
vectx-core = { version = "0.2.8", path = "lib/core" }
vectx-storage = { version = "0.2.8", path = "lib/storage" }
vectx-api = { version = "0.2.8", path = "lib/api" }

[dev-dependencies]
tempfile = "3.0"
rand = "0.9"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3

[profile.dev]
debug = "line-tables-only"
